var box = document.createElement('div');
document.body.appendChild(box);
box.innerText = '基础类型';

let isDone: boolean = false; // 布尔型
let decLiteral: number = 5; // 数字型
let nameD: string = "bob"; // 字符串类型
nameD = "smith"
let list: number[] = [1,2];
